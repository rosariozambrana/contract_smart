import 'package:flutter/material.dart';
class BuildImageList extends StatelessWidget {
  const BuildImageList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
